<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.a.b.dao.CommentDao">

	<insert id="commentWrite">
	INSERT INTO COMMENT(
	BCID, BBID, BID, CCONTENT, CDATE
	) VALUES(
	COMMENT_SEQ.NEXTVAL, #{param1,jdbcType=VARCHAR}, #{param2,jdbcType=VARCHAR}, 
	sysdate
	)
	</insert>
	
	<select id="commentContent_view" resultType="com.a.b.dto.Comment" parameterType="long">
	SELECT * FROM
	COMMENT
	WHERE BCID = #{param1}
	</select>
	
	<select id="commentList" resultType="com.a.b.dto.Comment">
		SELECT BCID, BBID, BID, CCONTENT, CCDATE
		FROM COMMENT
	</select>
	
	<update id="commentModify">
	UPDATE COMMENT
	SET BID = #{param2,jdbcType=VARCHAR}, CCONTENT = #{param3,jdbcType=VARCHAR}
	WHERE BCID = #{param1,jdbcType=NUMERIC}
	</update>
	
	<delete id="commentDelete" parameterType="long">
	DELETE FROM COMMENT WHERE BCID = #{param1}
	</delete>
	
</mapper>